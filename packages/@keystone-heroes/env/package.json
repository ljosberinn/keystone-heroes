{
  "name": "@keystone-heroes/env",
  "version": "1.0.0",
  "private": true,
  "main": "src",
  "scripts": {
    "format": "prettier --write ./src/index.ts",
    "copy-env-to-db": "cp .env ../db/.env",
    "lint": "eslint . --cache",
    "test": "exit 0"
  },
  "devDependencies": {
    "dotenv": "10.0.0",
    "typescript": "4.3.2"
  }
}
